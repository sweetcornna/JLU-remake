# 第1章 操作系统概述

整理自 `笔记/操作系统原理笔记.pdf` 第1章，1.2 节补入了 `笔记/操作系统期末整理.pdf` 第一章的分类内容。笔记正文只做了机械清洗和改错，章首要点、章末对比表、易错点和典型题是整理时写的。

## 本章要点

- 复习范围是课件 1.4 操作系统类型、1.5 操作系统运行环境。笔记的编号和课件不一样：课件 1.4 对应下面的 1.2，课件 1.5 对应下面的 1.3。
- 配套讲义：[第1章 操作系统概述（重制版）](<../重制版PPT/第1章 操作系统概述（重制版）.pptx>)，原课件 `课件/01第一章 操作系统概述.pptx`
- **操作系统**：控制和管理整个计算机系统的软硬件资源、合理组织计算机工作流程、为用户和上层软件提供接口的程序集合，是最接近硬件的一层系统软件。
- 四个基本特征：并发、共享、虚拟、异步。并发和共享互为存在条件，没有并发就谈不上虚拟。
- 操作系统类型：多道批处理（脱机，多道、成批）、分时（联机，多路性、交互性、独占性）、实时（及时性、可靠性）、通用、单用户、网络、分布式、多处理器、嵌入式等。
- 硬件环境：定时装置（绝对时钟、间隔时钟）、堆与栈（一个堆，用户栈和系统栈两个栈）、寄存器（PSW、PC、SP、通用、浮点、地址映射寄存器）、特权指令、处理器状态、地址映射机构、存储保护、中断装置、通道与 DMA。
- **管态与目态**：由 PSW 中的一位标识。目态转管态的唯一途径是中断；管态转目态靠置 PSW（中断返回指令），这条指令是特权指令。
- 系统调用通过访管（陷入）指令进入内核。访管指令在目态下执行，所以它本身不是特权指令。
- 中断（外中断）来自 CPU 执行指令之外，异常（内中断）来自指令执行内部；异常不能屏蔽。

---

## 1.0 引论

- **操作系统与用户界面**：虽然用户通常通过shell（基于文本的用户界面）或GUI（图形用户界面）与操作系统交互，但这些界面实际上并不是操作系统的一部分。它们是运行在用户态的程序，使用操作系统提供的服务来完成工作。
- **内核态与用户态**：大多数计算机系统有两种运行模式：内核态（管态、核心态、系统态）和用户态（目态）。操作系统运行在内核态，具有对所有硬件的完全访问权，可以执行任何机器指令。而用户态程序只能使用指令集的一个子集，不能直接执行会影响机器控制或进行I/O操作的指令。
- **操作系统与用户程序的区别**：操作系统和用户程序的主要区别在于它们的地位和功能。用户可以选择或自己编写他们喜欢的用户程序，但不能修改操作系统的部分，因为这部分由硬件保护，防止用户试图对其进行修改。
- **嵌入式系统和解释系统**：在某些特殊的系统中，如嵌入式系统（没有内核态）和解释系统（如基于Java的操作系统，采用解释方式而非硬件方式区分组件），操作系统和用户程序的区别可能会变得模糊。
- **用户态程序的特权功能**：在许多系统中，一些在用户态下运行的程序协助操作系统完成特权功能，例如修改用户口令。这些程序虽然不是操作系统的一部分，也不在内核态下运行，但它们具有敏感的功能，并必须以某种方式得到保护。
- **操作系统的特性**：与用户程序相比，操作系统地位更高。除此之外，操作系统通常更大、更复杂，并且寿命更长。

## 1.1 操作系统的基本概念

### 1.1.1 操作系统的概念和目标

**（1）概念：**

操作系统（Operating System，OS）是指**控制和管理整个计算机系统的硬件与软件资源**，合理地**组织、调度计算机的**工作与资源的分配**，进而为用户和其他软件**提供方便接口与环境的程序集合。操作系统是计算机系统中最基本的**系统软件**。【操作系统是最接近硬件的一层软件】

分点总结如下：
- 是系统最基本最核心的软件，属于系统软件
- 控制和管理整个计算机的硬件和软件资源【系统资源的管理者】
- 合理的组织、调度计算机的工作与资源的分配【最接近硬件的一层软件】
- 为用户和其它软件提供方便的接口和环境【向上层提供方便易用的服务】

**（2）计算机的层次结构（操作系统的位置）**

原稿这里和下面（3）的图在导出的 PDF 里缺失，只剩标题。

**（3）操作系统的功能和目标【王道版】**

下面详细展开叙述：

**①** **作为计算机系统资源的管理者**
1. **处理机管理**：
  - 在多道程序环境下，处理机（CPU）的分配和运行都以进程（或线程）为基本单位，对处理机的管理可归结为对进程的管理。
  - 进程管理的主要任务是对进程的创建、撤销、调度、同步、通信和避免冲突进行管理。
  - 进程管理的功能包括进程控制、进程同步、进程通信、死锁处理和处理机调度。
2. **存储器管理**：
  - 存储器管理旨在为多道程序的运行提供良好的环境，方便用户使用并提高内存的利用率。
  - 主要功能包括内存分配与回收、地址映射、内存保护与共享以及内存扩充。
3. **文件管理**：
  - 计算机中的信息以文件的形式存在，操作系统中负责文件管理的部分称为文件系统。
  - 文件管理包括文件存储空间的管理、目录管理以及文件读写管理和保护。
4. **设备管理**：
  - 设备管理的主要任务是满足用户的I/O请求，方便用户使用各种设备，并提高设备的利用率。
  - 主要功能包括缓冲管理、设备分配、设备处理和虚拟设备。

**②** **作为用户与计算机硬件系统之间的接口**
- **命令接口**：命令接口是用户通过输入操作命令来组织和控制作业的执行。它分为联机命令接口和脱机命令接口，**用户可以直接调用**
  - **联机命令接口**：也称为交互式命令接口，适用于分时或实时系统。用户通过控制台或终端输入操作命令，向系统提出服务要求。【说一句做一句】例如，在Unix或Linux系统中，用户可以在命令行终端输入各种命令，如`ls` 、`cd` 、`mkdir`等，来查看文件、切换目录或创建目录。
  - **脱机命令接口**：也称为批处理命令接口，适用于批处理系统。用户不能直接干预作业的运行，而是事先用作业控制命令写成一份作业操作说明书，连同作业一起提交给系统。【说一堆做一堆】例如，在Windows系统中，用户可以编写批处理脚本（.bat文件），在脚本中编写一系列的命令，然后一次性执行这个脚本。
- **程序接口**：程序接口由一组**系统调用**（也称广义指令，后面会解释）组成，用户可以在程序中使用这些系统调用来请求操作系统服务，如使用外部设备、申请分配和回收内存等。用户通过程序间接使用的，编程人员可以使用它们来请求操作系统服务例如，在C语言编程中，用户可以使用`malloc` 和`free` 来申请和回收内存（更正：原文说 `malloc` 和 `free` 是系统调用。它们其实是C库函数，库函数内部再通过 `brk`、`mmap` 这类系统调用向操作系统要内存）。
- **图形用户界面（GUI）**：当前最流行的用户接口是图形用户界面，用户可以通过鼠标和键盘在图形界面上进行操作，如单击或使用快捷键，从而方便地使用操作系统。虽然严格来说，**图形接口不是操作系统的一部分**，但图形接口所调用的系统调用命令是操作系统的一部分。例如，在Windows系统中，用户可以通过单击“开始”菜单，然后选择“关机”选项来关闭计算机。

**③** **实现了对计算机资源的扩充**
- 没有任何软件支持的计算机称为**裸机**
- 覆盖了软件的机器称为**扩充机器**或**虚拟机**

操作系统所提供的资源管理功能和方便用户的各种服务功能，将裸机改造成功能更强、使用更方便的机器

### 1.1.2 操作系统的特征

操作系统的基本特征包括**并发**、**共享**、**虚拟**和**异步**。

**（1）并发(Concurrence)**
- **并发：**两个或多个事件在**同一时间间隔内**发生，这些事件在宏观上是同时发生的，在微观上是交替发生的， 操作系统的并发性指系统中同时存在着多个运行的程序
- **并行：**两个或多个事件在**同一时刻**发生
- 一个单核（CPU）同一时刻只能执行一个程序，因此操作系统会协调多个程序使他们交替进行（这些程序在宏观上是“并行”，在微观上串行）
- 并发是随多道程序设计出现的，20世纪60年代的多道批处理系统（如 IBM OS/360）已经让多道程序并发执行（更正：原文说 UNIVAC I、IBM System/360 没有并发支持，到70年代末80年代初的 UNIX、MS-DOS、VMS 才开始支持。OS/360 已经支持多道程序，MS-DOS 反而是单任务系统）
- 在如今的计算机中，一般都是多核cpu的，即在同一时刻可以并行执行多个程序，比如我的计算机是12核的，我的计算机可以在同一时刻并行执行12个程序，但是事实上我们计算机执行的程序并不止12个，因此并发技术是必须存在的，并发性必不可少。
- 原稿作者在任务管理器里看到自己笔记本的CPU是12个内核、16个逻辑处理器，逻辑处理器数就是同一时刻最多能并行执行的线程数，运行的程序比这多，就只能靠并发轮流执行。（整理注：原稿这里逐项解释了基准速度、插槽、虚拟化、三级缓存等参数，是聊天机器人生成的，和本课关系不大，已压缩。原文说“逻辑处理器是物理核心的两倍”，16 并不是 12 的两倍，这类CPU的大核支持超线程、小核不支持，所以数目对不上。）

**（2）共享(Sharing)**
- 资源共享即共享，是指系统中的**资源可以供内存中多个并发执行的进程共同使用**
- 共享分为两类：**互斥共享**和**同时共享**

**①** **互斥共享**
- 计算机中的某个资源在一段时间内只能允许**一个进程**访问，别的进程没有使用权
- 临界资源（独占资源）：在一段时间内只允许一个进程访问的资源。计算机中大多数物理设备及某些软件中的栈、变量和表格都属于临界资源，它们被要求互斥共享
- 例：比如QQ和微信视频。同一段时间内摄像头只能分配给其中一个进程

**②** **同时共享**
- 计算机中的某个资源在在一段时间内可以**同时允许多个进程**访问
- 同时共享通常要求一个请求分为几个时间片段间隔的完成，即交替进行，“分时共享”
- 这里的**“同时”**指在宏观上是同时的，在微观上是交替进行访问的，只是CPU处理速度很快，我们感觉不到
- 例：比如QQ在发送文件A，微信在发送文件B，宏观上两个进程A和B都在访问磁盘，在我们看来是同时进行的，但是在微观上两个进程A和B是交替进行访问磁盘的
- **注意**：有时候多个进程可能真的是在同时进行资源访问，比如玩游戏时可以放音乐，游戏声音和音乐声音都能听见

**并发和共享是操作系统两个最基本的特征，两者之间互为存在的条件：**

①资源共享是以程序的并发为条件的，若系统不允许程序并发执行，则不存在资源共享问题；

②若系统不能对资源共享实施有效的管理，则必将影响到程序的并发执行，甚至根本无法并发执行。

**（3）虚拟(Virtual)**
- 虚拟是把一个物理上的实体变为若干逻辑上的对应物。
- 物理实体（前者）是实际存在的；而后者是虚的，是用户感觉上的事务
- 虚拟技术：用于实现虚拟的技术
- 虚拟处理器（CPU）：通过多道程序设计技术，采用让多道程序并发执行的方法，分时来使用一个CPU，实际物理上只有一个CPU，但是用户感觉到有多个CPU
- 虚拟存储器：从逻辑上扩充存储器容量，用户感觉到的但实际不存在的存储器。虚拟存储器允许程序认为它们拥有比物理内存更多的内存。这是通过将内存中不常用的部分移动到硬盘上的交换空间来实现的。当这些部分再次被需要时，操作系统会将它们从硬盘调回内存。【硬盘（磁性存储设备）是计算机的主要存储设备，用于长期存储数据。它的存储容量通常很大，可以达到数TB。硬盘上存储的数据在电源关闭后仍然存在。内存（RAM）是计算机的临时存储设备，用于存储正在运行的程序和数据。它的存储容量相对较小，但读写速度非常快。内存中的数据在电源关闭后会丢失。】
- 虚拟设备：将一台物理设备虚拟为逻辑上的多台设备，使多个用户在同一时间段内访问同一台设备，即同时共享，用户宏观上感觉是同时的，但实际上是微观交替访问同一台设备的
- **操作系统的虚拟技术可归纳为：**
  - **时分复用技术：如处理器的分时共享**
  - **空分复用技术：如虚拟存储器**

显然，如果失去了并发性，则一个时间段内系统中只需运行一道程序，那么就失去了实现虚拟性的意义了。因此，**没有并发性，就谈不上虚拟性**

**（4）异步(Asynchronism)**
- 异步：多道程序环境允许多个程序**并发**执行，但由于资源有限，如CPU只有一个，进程的执行走走停停，以不可预知的速度向前推进。
- 比如A进程正在占用CPU计算，B进程这时也想占用CPU计算，B进程只有等，等A进程算完了，A进程去访问磁盘资源了，这时B进程再占用CPU进行计算，B进程还没计算完，A进程从磁盘取出资源了，A进程发现B这时在占用CPU，这时A进程就需要等待，等B算完后再继续到CPU中进行计算。由于每个进程占用资源的时间不固定，所以进程的执行以不可预知的速度前进

## 1.2 操作系统分类

笔记原稿这一节是空的，下面的内容来自 `笔记/操作系统期末整理.pdf` 第一章。

**操作系统的作用：**

① 管理系统中的各种资源

② 为用户提供友好的界面

**操作系统的产生：**
- 手动操作阶段
- 批处理阶段
  - 联机批处理
  - 脱机批处理
- 执行系统阶段
  - 硬件在两个方面取得了重要的进展。一是引入通道，二是出现通道中断主机功能
  - 假脱机

通道又称I/O处理器

**操作系统的完善（发展过程）：**
- 多道批处理系统
- 分时系统
- 实时处理系统
- 通用操作系统

**操作系统的分类：**
- 多道批处理操作系统
  - 以脱机操作为标志
  - 两个标志
    - 多道
    - 成批
- 分时操作系统
  - 是以联机操作为标志，适合于程序的动态调试和修改
  - 3个重要的特性
    - 多路性
    - 交互性
    - 独占性
- 实时操作系统
  - 所谓实时，是指系统能够对外部请求做出及时的响应。
  - 2个特性
    - 及时性
    - 可靠性
  - 按其应用范围可以分为
    - 实时控制
    - 实时信息处理
- 通用操作系统
  - 同时具有分时、实时和批处理功能的操作系统
  - 可能同时存在3类任务。这3类任务通常按照其急迫程度加以分级：实时任务级别最高，分时任务级别次之，批处理任务级别最低
    - 实时任务
    - 分时任务
    - 批处理任务
- 单用户操作系统
  - 最主要的特点是单用户，即系统在同一段时间内仅为一个用户提供服务
- 网络操作系统
  - 用于实现网络通信和网络资源管理的操作系统
  - 两个目的：
    - 相互通信
    - 共享资源
- 分布式操作系统
- 多处理器操作系统
- 集群操作系统
- 云计算操作系统
- 嵌入式操作系统
- 多媒体操作系统
- 智能卡操作系统

各类系统的详细特点见 `11 简答题精编（第1-3章）.md` 第1章“操作系统有哪些类型？各自有什么特点？”。

## 1.3 操作系统运行环境

这一部分我将结合王道和吉林大学的教材中的“操作系统的硬件环境”进行补充，可能看起来有点冗长，但是感觉都是一些必备的基础概念

### 1.3.1 定时装置

1. **绝对时钟**：类似于电子表，其时间表现形式为年：月：日：时：分：秒。绝对时钟的值保存于硬件寄存器中，开机时由电源供电计时，关机时由机内电池供电计时。其值可由程序设定和修改，但一般通过特权指令完成。操作系统需要根据绝对时钟的值记录作业进入系统和处理的时间、文件的修改和存取时间、资源占用时间、日志记录时间等。
2. **间隔时钟**：也称为闹钟，每隔固定的时间，如10 ms，发生一次时钟中断。时钟中断发生后，操作系统获得系统的控制权，以便运行系统管理和实现程序并发。利用间隔时钟还可以实现逻辑时钟，例如，要实现一个50 ms的逻辑时钟，可以设置一个初值为5的变量，每次时钟中断将变量值减1，当其值减至0时即达到50 ms。

### 1.3.2 堆与栈

每个运行程序都有一个堆和两个栈（一个用户栈，一个系统栈）。
1. **堆**：属于用户空间，用于保存程序中的**动态变量**。堆空间由操作系统分给运行程序，其大小需求不定。操作系统一般为运行程序分配一个基本大小的堆，如8 KB，如果程序运行时对堆空间的需求没有超过8 KB，则不需要操作系统进一步的帮助，对堆空间的使用由应用软件管理；但如果用户程序运行时对堆的需求超过了8 KB，则会发生中断，操作系统为其扩充堆空间。
2. **用户栈**：属于用户空间，用于保存用户函数调用时的返回点、参数、局部变量、返回值。用户程序调用操作系统时，有两个载体可以用来传递参数：寄存器和用户栈。小的数据可以通过寄存器传递，较长的参数寄存器放不下，只能通过用户栈传递。
3. **系统栈**：也称核心栈，系统栈在逻辑上属于操作系统空间，主要有两个用途：一是保存操作系统子程序间相互调用的返回点、参数、局部变量、返回值；二是**中断响应时保存中断现场**，包括PSW和PC，以及中断处理过程中用到的寄存器值。对于嵌套中断，被中断程序的现场信息依次压入系统栈，中断返回时逆序弹出。

程序切换的同时伴随着堆、用户栈和系统栈的切换（后文会详细叙述），但硬件的栈指针是多个进程共享的。

### 1.3.3 寄存器

1. **程序状态字寄存器（PSW）**：程序状态字寄存器，用于存储程序的当前状态信息，如程序的运行模式（用户模式或内核模式）、中断使能位、条件码等。处理器靠这些信息控制程序的运行。
2. **指令计数器（PC）**：也称为程序计数器，用于存储下一条要执行的指令的地址。当一条指令执行完毕后，PC会自动指向下一条指令，除非有跳转指令改变PC的值。
3. **栈指针（SP）**：栈指针寄存器，用于指向当前栈顶的地址。每当有新的数据入栈时，栈指针就会相应地移动。内核态和用户态各一个，分别保存系统栈和用户栈的栈顶位置。
4. **通用寄存器（regs）**：这些寄存器可以用于存储临时数据，或者作为指令的操作数。在不同的体系结构中，通用寄存器的数量和功能可能会有所不同。
5. **浮点寄存器（fregs）**：用于存储浮点数值和进行浮点运算。由于浮点运算的特殊性，这些寄存器通常有专门的硬件支持。
6. **地址映射寄存器**：一般有一对，分别记录内存区域的起始地址和长度，分别称为基址寄存器(base)和限长寄存器(limit)。

### 1.3.4 特权指令与非特权指令

1. **特权指令**：只能在管态下才能执行的指令称为特权指令。这些指令包括开关中断、修改地址映射寄存器、置程序状态字、停机等。这些指令的执行不仅影响运行程序本身，也会影响其他程序，甚至整个系统。这些指令一般只有操作系统才能执行，而一般用户程序不可执行。
2. **非特权指令**：在管态和用户态下均可执行的指令称为非特权指令。这些指令的执行只与运行程序本身有关，不会影响其他程序和操作系统。例如，数据传送指令、算术运算指令等都属于非特权指令。
  这两类指令的区别在于它们的使用权限和影响范围。特权指令通常涉及到系统级的操作，因此需要更高的权限才能执行。而非特权指令则主要涉及到程序自身的运行，不会影响到系统的其他部分。这种设计是为了保护系统的稳定性和安全性，防止用户程序误操作或恶意攻击导致系统崩溃。

### 1.3.5 处理器状态及状态转换

1. **处理器状态**：为了构造一个可靠的系统，硬件至少要区分两种状态：管态和用户态。这两种状态由程序状态字中的一位标识。
  - **管态**：也称为系统态或核心态，是操作系统运行时所处的状态。在管态下，可以执行硬件所提供的全部指令，包括特权指令和非特权指令。在管态下可以改变机器状态，从而可以由管态转换为用户态。
  - **用户态**：是一般用户程序运行时所处的状态。在用户态下，只能执行硬件机器指令的一个子集，即非特权指令。如果用户程序在用户态下执行特权指令，硬件将产生中断，进入操作系统，特权指令的执行将被制止。这样就可以防止用户有意或无意地侵入系统，从而起到系统保护的作用。
  现代计算机（如 x86）支持的保护级别多达4级，即R0～R3，其中R0的权限最高，R3的权限最低。不过目前建立在计算机上的操作系统都只用了R0和R3这两个状态，操作系统运行于R0，用户程序运行于R3（更正：原文作“多达5级，即R0～R4”，用户程序运行于R4。x86 的保护环是 R0～R3 四级）。
2. **状态转换**：在系统运行的过程中，处理器状态处于动态变化之中，即时而处于用户态，时而处于管态，这种变化是有规律的。
  - **用户态到管态的转换**：由于修改处理器状态字指令属于特权指令，只能在管态下执行，因而用户态程序无法直接控制处理器状态的转换。处理器状态由用户态转换为管态的唯一途径是中断。中断发生时，中断向量中的处理器状态字应标识处于管态，这个标识一般是由操作系统初始化程序来设置的。
  - **管态到用户态的转换**：管态到用户态的转换可以通过修改程序状态字（置PSW）来实现。由于操作系统运行于管态，用户程序运行于用户态，因而这种状态转换伴随着由操作系统到用户程序的转换。

### 1.3.6 处理器运行模式

  现代操作系统通常采用分层式结构，各项功能被设置在不同的层次上。一些与硬件关联较紧密的模块，如时钟管理、中断处理、设备驱动等处于最低层。其次是运行频率较高的程序，如进程管理、存储器管理和设备管理等。这两部分内容构成了操作系统的内核。这部分内容的指令操作工作在核心态。
  **内核**是计算机上配置的底层软件，它管理着系统的各种资源，可以看作是连接应用程序和硬件的一座桥梁。大多数操作系统的内核包括以下四个方面的内容：
1. **时钟管理**：在计算机的各种部件中，时钟是最关键的设备。时钟的第一功能是计时，操作系统需要通过时钟管理，向用户提供标准的系统时间。另外，通过时钟中断的管理，可以实现进程的切换。
2. **中断机制**：引入中断技术的初衷是提高多道程序运行环境中 CPU 的利用率，而且主要是针对外部设备的。后来逐步得到发展，形成了多种类型，成为操作系统各项操作的基础。
3. **原语**：系统中的设备驱动、CPU切换、进程通信等功能中的部分操作都可定义为原语，使它们成为内核的组成部分。按层次结构设计的操作系统，底层必然是一些可被调用的公用小程序，它们各自完成一个规定的操作。它们处于操作系统的最底层，是最接近硬件的部分。这些程序的运行具有原子性，其操作只能一气呵成(出于系统安全性和便于管理考虑)。
4. **系统控制的数据结构及处理**：系统中用来登记状态信息的数据结构很多，如作业控制块、进程控制块(PCB)、设备控制块、各类链表、消息队列、缓冲区、空闲区登记表、内存分配表等。为了实现有效的管理，系统需要一些基本的操作。常见的操作有以下3种：（1）进程管理。进程状态管理、进程调度和分派、创建与撤销进程控制块等。（2）存储器管理。存储器的空间分配和回收、内存信息保护程序、代码对换程序等。（3）设备管理。缓冲区管理、设备分配和回收等。

### 1.3.7 中断和异常

在操作系统中，处理器的运行模式通常有两种：核心态和用户态。操作系统内核工作在核心态，而用户程序工作在用户态。为了从用户态进入核心态，需要在核心态建立一些“门”。在实际操作系统中，CPU运行上层程序时唯一能进入这些“门”的途径就是通过中断或异常。
1. **中断和异常的定义**：
  - **中断**：也称外中断，是指来自 CPU 执行指令外部的事件，通常用于信息输入输出，如设备发出的I/O 结束中断，表示设备输入/输出处理已经完成。时钟中断，表示一个固定的时间片已到，让处理机处理计时、启动定时运行的任务等。
  - **异常**：也称内中断，是指来自CPU执行指令内部的事件，如程序的非法操作码、地址越界、运算溢出、虚存系统的缺页及专门的陷入指令等引起的事件。异常不能被屏蔽，一旦出现，就应立即处理。
2. **中断和异常的分类**：
  - **外中断**：可分为可屏蔽中断和不可屏蔽中断。可屏蔽中断是指通过INTR线发出的中断请求，通过改变屏蔽字可以实现多重中断，从而使得中断处理更加灵活。不可屏蔽中断是指通过NMI线发出的中断请求，通常是紧急的硬件故障，如电源掉电等。此外，异常也是不能被屏蔽的。
  - **异常**：可分为故障、自陷和终止。故障通常是由指令执行引起的异常，如非法操作码、缺页故障、除数为 0、运算溢出等。自陷是一种事先安排的“异常”事件，用于在用户态下调用操作系统内核程序，如条件陷阱指令。终止是指出现了使得 CPU无法继续执行的硬件故障，如控制器出错、存储器校验错等。故障异常和自陷异常属于软件中断(程序性异常),终止异常和外部中断属于硬件中断。
3. **中断和异常的处理过程**：当CPU在执行用户程序的第i条指令时检测到一个异常事件，或在执行第i条指令后发现一个中断请求信号，则CPU打断当前的用户程序，然后转到相应的中断或异常处理程序去执行。若中断或异常处理程序能够解决相应的问题，则在中断或异常处理程序的最后，CPU通过执行中断或异常返回指令，回到被打断的用户程序的第i条指令或第i+1条指令继续执行：若中断或异常处理程序发现是不可恢复的致命错误，则终止用户程序。通常情况下，对中断和异常的具体处理过程由操作系统(和驱动程序)完成。

（下面几小节偏计算机组成原理，原稿作者顺带复习了一遍。）

**（1）多重中断**
  多重中断，也称为中断嵌套，是指在CPU执行某个中断服务程序的过程中，如果发生了新的更高级的中断请求，CPU会暂停当前的中断处理，转去处理新的中断。待新的中断处理完毕后，CPU再返回继续处理原来的中断。

这种机制的实现需要满足以下条件：
- 在现行中断服务期间，中断允许触发器为1，即开中断。
- 若新的中断的优先级高于原中断的优先级，CPU响应新的中断；否则，CPU不予响应，必须待原中断处理完毕且返回主程序后，再响应新的中断。

这种机制可以提高系统的响应速度和处理能力，特别是在处理多个并发事件时，能够确保更高优先级的事件能够得到及时处理。

**（2）处理中断的过程**
- **中断请求**：首先，一个外部设备（如键盘、鼠标或网络接口）或内部事件（如定时器或软件中断）会向处理器发出一个中断请求。
- **中断检测**：处理器在每条指令执行完毕后，会检查是否有中断请求。如果有，处理器会暂停当前正在执行的任务。
- **保存现场**：处理器会保存当前任务的状态，包括程序计数器（PC）、程序状态字（PSW）以及其他必要的寄存器内容。这些信息被保存在栈中，以便在中断处理完毕后可以恢复到中断前的状态。
- **中断服务程序**：处理器根据中断向量表（一个存储了各种中断处理程序地址的表）找到对应的中断服务程序，并开始执行。这个程序会处理中断请求，例如，如果是键盘中断，中断服务程序可能会读取键盘输入并将其存入缓冲区。
- **恢复现场**：一旦中断服务程序执行完毕，处理器会从栈中恢复之前保存的任务状态，包括程序计数器、程序状态字以及其他寄存器的内容。
- **返回**：处理器返回到被中断的任务，继续执行下一条指令。

### 1.3.8 系统调用

**定义：**系统调用是用户在程序中调用操作系统所提供的一些子功能，可以视为特殊的公共子程序。

**类别：**系统调用的种类大致可以分为以下几类：
1. **设备管理**：完成设备的请求或释放，以及设备启动等功能。
2. **文件管理**：完成文件的读、写、创建及删除等功能。
3. **进程控制**：完成进程的创建、撤销、阻塞及唤醒等功能。
4. **进程通信**：完成进程之间的消息传递或信号传递等功能。
5. **内存管理**：完成内存的分配、回收以及获取作业占用内存区大小及始址等功能。

**用户态和核心态**
  用户程序不能直接执行对系统影响非常大的操作，必须通过系统调用的方式请求操作系统代为执行，以便保证系统的稳定性和安全性，防止用户程序随意更改或访问重要的系统资源，影响其他进程的运行。
  用户程序可以执行陷入指令(又称访管指令或 trap指令)来发起系统调用，请求操作系统提供服务。

用户程序执行“陷入指令",相当于把CPU的使用权主动交给操作系统内核程序(CPU 状态会从用户态进入核心态),之后操作系统内核程序再对系统调用请求做出相应处理。处理完成后，操作系统内核程序又会把CPU 的使用权还给用户程序(即CPU状态会从核心态回到用户态)。

**用户态转向核心态的例子**
1. 用户程序要求操作系统的服务，即系统调用。
2. 发生一次中断。
3. 用户程序中产生了一个错误状态。
4. 用户程序中企图执行一条特权指令。
5. 从核心态转向用户态由一条指令实现，这条指令也是特权命令，一般是中断返回指令。

**注意：**由用户态进入核心态，不仅状态需要切换，而且所用的堆栈也可能需要由用户堆栈切换为系统堆栈，但这个系统堆栈也是属于该进程的。若程序的运行由用户态转到核心态，则会用到访管指令，访管指令是在用户态使用的，所以它不可能是特权指令。

原稿后面还有 1.4 操作系统结构、1.5 操作系统引导、1.6 虚拟机三个标题，都标着“待补充”，没有内容，这里删去。

## 1.7 第1章疑难点

**1.并行性与并发性的区别和联系**
- **并行性**：指两个或多个事件在同一时刻发生。
- **并发性**：指两个或多个事件在同一时间间隔内发生。
- 在多道程序环境下，宏观上有多个程序同时运行，但在单处理器系统中每个时刻却仅能有一道程序执行，因此微观上这些程序只能分时地交替执行，这就是并发性。
- 若在计算机系统中有多个处理器，则这些可以并发执行的程序便被分配到多个处理器上，实现并行执行，即利用每个处理器来处理一个可并发执行的程序。

**2.特权指令与非特权指令**
- **特权指令**：有特殊权限的指令，如清内存、置时钟、分配系统资源、修改虚存的段表或页表、修改用户的访问权限等。这类指令只能用于操作系统或其他系统软件，不直接提供给用户使用。特权指令必须在核心态执行。
- **非特权指令**：在用户态下可以执行的指令。为了防止用户程序中使用特权指令，用户态下只能使用非特权指令，核心态下可以使用全部指令。在用户态下使用特权指令时，将产生中断以阻止用户使用特权指令。

**3. 访管指令与访管中断**
- **访管指令**：一条可以在用户态下执行的指令。在用户程序中，因要求操作系统提供服务而有意识地使用访管指令，从而产生一个中断事件(自愿中断)，将操作系统转换为核心态，称为访管中断。
- **访管中断**：由访管指令产生，程序员使用访管指令向操作系统请求服务。处于用户态的用户程序使用访管指令时，系统根据访管指令的操作数执行访管中断处理程序，访管中断处理程序将按系统调用的操作数和参数转到相应的例行子程序。完成服务功能后，退出中断，返回到用户程序断点继续执行。访管指令本身不是特权指令，其基本功能是让程序拥有“自愿进管”的手段，从而引起访管中断。

---

## 易混对比

| | 管态（核心态、系统态） | 目态（用户态） |
|---|---|---|
| 运行的程序 | 操作系统 | 用户程序 |
| 可执行的指令 | 全部指令，含特权指令 | 只有非特权指令 |
| 怎么进入 | 中断（包括访管指令引起的自愿性中断） | 置 PSW，一般是中断返回指令 |
| 用哪个栈 | 系统栈 | 用户栈 |

| | 多道批处理 | 分时 | 实时 |
|---|---|---|---|
| 操作方式 | 脱机 | 联机 | 联机，常是专用系统 |
| 主要追求 | 系统效率、吞吐量 | 交互性，响应时间一般为秒级 | 及时响应（毫秒、微秒级）和可靠性 |
| 特性 | 多道、成批 | 多路性、交互性、独占性 | 及时性、可靠性 |
| 用户界面 | 作业控制语言 JCL | 交互终端命令或 GUI | 随应用而定 |

| | 并发 | 并行 |
|---|---|---|
| 含义 | 同一时间间隔内发生，宏观同时、微观交替 | 同一时刻发生 |
| 单处理器上 | 进程之间可以并发 | 进程与进程之间不可能并行，但处理器与设备、处理器与通道之间可以并行 |

| | 外中断（中断） | 内中断（异常） |
|---|---|---|
| 来源 | CPU 执行指令以外的事件：I/O 结束、时钟 | 指令执行本身：非法操作码、越界、溢出、缺页、陷入指令 |
| 能否屏蔽 | INTR 线上的可屏蔽，NMI 线上的不可屏蔽（如掉电） | 不能屏蔽 |
| 返回位置 | 通常返回下一条指令 | 故障类重新执行本条指令，自陷返回下一条 |

## 易错点

- 访管指令不是特权指令；开关中断、置 PSW、修改地址映射寄存器、I/O 指令、停机是特权指令。
- “中断发生后，硬件保证处理器进入管态”这句话不准确。中断后是否处于管态，取决于中断向量里 PSW 的状态位，这一位由操作系统初始化程序设置。
- 中断向量的**存放位置**由硬件决定，**内容**（中断处理程序的 PSW 和 PC）由操作系统决定。
- 图形界面本身不算操作系统的一部分，GUI 背后调用的系统调用才是。
- `malloc`、`free` 是库函数，不是系统调用（笔记原文把它们说成了系统调用，已在正文里更正）。
- 系统栈属于操作系统空间，保存中断现场和内核函数调用信息；用户栈属于用户空间。进程切换时两个栈都跟着切换。

## 典型题

- 简答：`11 简答题精编（第1-3章）.md` 第1章。常考的有“操作系统有哪些类型”“管态和目态的划分有什么好处”“何谓特权指令”“中断向量的存储位置和内容由谁决定”“系统如何由目态转换为管态”“中断与程序并发的关系”“系统栈与用户栈”。
- 名词解释：`10 名词解释.md` 第1章，笔记附录的往届名词里有 Operating System、System Calls、Multiprogramming。
- 教材习题一的题目清单和对应答案位置见 `20 往届简答与教材习题索引.md`。
