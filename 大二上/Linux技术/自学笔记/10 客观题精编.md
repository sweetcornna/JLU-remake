# 客观题精编

整理自 `Linux整理（客观题）.docx`，共 80 道单选题（1 到 80）和 60 道判断题（124 到 183）。原稿题号从 80 直接跳到 124，中间的题没有收录。这里按主题重新归类，题号保留原稿编号，方便和 `03 vi与常用命令速查.md` 对照。

答案的来源分三种，每题都标出来了：

- 只写"答案：X"的，是原稿用黄色高亮标出的答案。单选 1 到 49 和判断 138、144、148、149、172 属于这一类。
- 写"答案：X（整理补充）"的，原稿没有标，整理时补上，后面附一句理由。
- 写"原卷无答案"的，原稿没有标，整理时也拿不准课程的标准答案，只给提示。

原稿把 13、23、76、78、130 这几题的题号标成了红色，判断 138、144、148、149 后面注了"bing 错了"，这里都照注。

## 一、Unix 与 Linux 概述

#### 单选 1

下面哪个操作系统不属于类 UNIX 操作系统？\
A. UNIX System V；B. Berkeley UNIX；C. Windows 10；D. Linux

答案：C

#### 单选 2

本门课程平时成绩和期末考试成绩的比例是？\
A. 3:7；B. 4:6；C. 5:5；D. 1:9

答案：B

#### 单选 3

POSIX 标准的作用是对 UNIX 进行（）。\
A. 标准化；B. 可定制化；C. 互联互通；D. 可移植化

答案：A

#### 单选 4

Linux 的内核在哪个开源协议下发行？\
A. Apache；B. GPL；C. BSD；D. MIT

答案：B

#### 单选 5

GNU 开发的工具在哪个协议下发行？\
A. Apache；B. GPL；C. BSD；D. MIT

答案：B

#### 单选 6

Linux 内核的开发者是？\
A. Richard Stallman；B. Elon Musk；C. Linus Torvalds；D. Jeff Bezos

答案：C

#### 判断 124

UNIX 从一开始就是用 C 语言编写的。

答案：错（整理补充）。UNIX 最初用汇编语言开发，1973 年才用 C 重写。

#### 判断 125

FSF（Free Software Foundation）组织中，Free 的含义是免费。

答案：错（整理补充）。Free 指自由。

#### 判断 126

用户修改在 GPL 协议下发布的软件，再发布时可以添加自己的限制条件。

答案：错（整理补充）。GPL 规定重新发行软件的人不能添加自己的限制。

#### 判断 127

UNIX 被注册为软件商标，最早由 AT&T 公司持有。

答案：对（整理补充）。狭义的 UNIX 是软件商标，先后由 AT&T、Novell、X/Open、The Open Group 持有。

#### 判断 128

Solaris、OpenBSD、Linux 都是类 UNIX 系统。

答案：对（整理补充）。Solaris 属于 System V 流派，OpenBSD 属于 BSD 流派，Linux 是类 UNIX 系统。

#### 判断 129

开源软件必须在 GPL 协议下发行。

答案：错（整理补充）。开源许可证还有 Apache、BSD、MIT、Mozilla、木兰公共许可证等。

## 二、登录、命令格式与 shell

#### 单选 7

检测命令，启动相应 Linux 程序的组件是？\
A. 内核；B. 文件系统；C. 虚拟计算机；D. shell

答案：D

#### 单选 8

Linux 系统中的管理员是？\
A. admin；B. root；C. adminstrator；D. sys

答案：B

#### 单选 9

Linux 中，命令提示符中的哪个符号可以说明用户是普通用户（）。\
A. `#`；B. `￥`；C. `$`；D. `&`

答案：C

#### 单选 10

用户登录成功后，命令行是由哪个系统组件显示出来的？\
A. 内核；B. 常驻模块；C. shell；D. 应用工具

答案：C

#### 单选 11

命令中选项的作用是（）。\
A. 调整命令的执行行为；B. 指明命令作用的对象；C. 加速命令执行；D. 调整命令优先级

答案：A

#### 单选 12

命令中参数的作用是（）。\
A. 调整命令的执行行为；B. 指明命令作用的对象；C. 加速命令执行；D. 调整命令优先级

答案：B

#### 单选 13（原稿题号标红）

下面选项的使用中，哪个是错误的？\
A. `ls -a`；B. `ls -a -l`；C. `ls -al`；D. `ls -a-l`

答案：D

原稿解析：多个短选项可以合写成 `-al`，也可以分开写成 `-a -l`，`-a-l` 这种把短横线夹在中间的写法是错的。

#### 单选 14

命令 `ls -a -l /boot` 中，参数是？\
A. `-a -l`；B. `/boot`；C. `boot`；D. `-a -l /boot`

答案：B

#### 单选 15

显示时间和日期的命令是？\
A. time；B. date；C. who；D. now

答案：B

#### 单选 16

用于显示当前系统中登录用户信息的命令是？\
A. login；B. who；C. logged；D. here

答案：B

#### 单选 17

类 UNIX 系统中的虚拟终端称为（）。\
A. vt；B. tty；C. pts；D. `:0`

答案：B

#### 单选 18

使用远程登录软件登录系统后，使用 who 命令看到该用户的登录终端类型为（）。\
A. 虚拟终端 tty；B. 控制台 console；C. 伪终端 pts；D. 远程终端

答案：C

#### 单选 19

在图形用户界面中使用终端模拟软件打开一个终端窗口，使用 who 命令看到该用户的登录终端类型为（）。\
A. 虚拟终端 tty；B. 控制台 console；C. 伪终端 pts；D. 本地终端

答案：C

#### 单选 20

用户 A 切换到虚拟终端 2 并成功登录，其他用户使用 who 命令看到用户 A 的登录终端为（）。\
A. 虚拟终端 tty；B. 控制台 console；C. 伪终端 pts/2；D. 虚拟终端 tty2

答案：D

#### 单选 21

显示日历信息的命令是？\
A. date；B. cal；C. calendar；D. ymd

答案：B

#### 单选 22

内部命令又称为（）。\
A. 磁盘命令；B. shell 命令；C. 系统工具程序；D. 内存命令

答案：B

#### 单选 23（原稿题号标红）

想终止正在命令行中执行的程序，可以使用组合键？\
A. Ctrl+Z；B. Ctrl+C；C. Ctrl+W；D. Ctrl+B

答案：B。Ctrl+Z 是挂起，容易选错。

#### 单选 24

shell 程序保存在哪个目录中？\
A. `/etc`；B. `/sbin`；C. `/bash`；D. `/bin`

答案：D

#### 判断 130（原稿题号标红）

shell 是命令解释器，位于 Linux 的内核中。

答案：错（整理补充）。shell 是命令解释器，但它运行在内核之外，负责用户与系统的交互。

#### 判断 131

用户在使用 Linux 系统前必须登录。

答案：对（整理补充）。Linux 是多用户系统，要先用用户名和密码登录才能使用。

#### 判断 132

在 Linux 中，root 用户可以使用 passwd 命令修改其他用户的密码。

答案：对（整理补充）。root 执行 `passwd 用户名` 即可。

#### 判断 133

命令行中的回车键解释为命令行的结束符。

答案：对（整理补充）。

#### 判断 134

命令中可以使用多个选项。

答案：对（整理补充）。例如 `ls -a -l`。

#### 判断 135

help 命令无法提供外部命令的帮助信息。

答案：对（整理补充）。help 只管内部命令，实验一里 `help ls` 就报错。

#### 判断 136

外部命令又称系统工具程序，保存在文件系统中，使用时才载入内存执行。

答案：对（整理补充）。

#### 判断 137

在命令行中输入新的 shell 名（该 shell 文件已在 `/bin` 目录中）并执行，可以临时改变 shell。

答案：对（整理补充）。实质是在当前 shell 的子进程里运行新 shell。

## 三、软件安装

#### 单选 25

使用 dnf 命令安装软件时的选项是（）。\
A. apt；B. setup；C. install；D. update

答案：C

#### 单选 26

使用 dnf 命令卸载软件时的选项是（）。\
A. uninstall；B. unsetup；C. delete；D. remove

答案：D

#### 单选 27

使用 dnf 命令安装软件包组时使用的选项是（）。\
A. teaminstall；B. groupinstall；C. groupadd；D. installall

答案：B

## 四、vi 编辑器

#### 单选 28

使 vi 进入命令模式的按键是？\
A. Tab；B. Esc；C. `c`；D. `i`

答案：B

#### 单选 29

重复上一次修改的按键是？\
A. `u`；B. `.`；C. `m`；D. `` ` ``

答案：B

#### 单选 30

移动光标到行首所用的键是？\
A. `.`；B. `w`；C. `0`；D. `$`

答案：C

#### 单选 31

移动光标到行尾所用的键是？\
A. `.`；B. `w`；C. `t`；D. `$`

答案：D

#### 单选 32

命令模式中，光标右移一个字所用的键是？\
A. `w`；B. `b`；C. `e`；D. `c`

答案：A

#### 单选 33

命令模式中，撤销最近一次修改所用的键（）。\
A. `c`；B. `b`；C. `u`；D. `U`

答案：C

#### 单选 34

命令模式中，撤销对当前行所做的所有修改所用的键是？\
A. `u`；B. `L`；C. `l`；D. `U`

答案：D

#### 单选 35

下面哪个命令不可以保存文件并退出 vi 编辑器？\
A. `:wq`；B. `ZZ`；C. `:save`；D. `:x`

答案：C

#### 单选 36

不保存文件，强制退出 vi 的命令是？\
A. `:break`；B. `:exit`；C. `:q!`；D. `:q`

答案：C

#### 单选 37

要进入末行模式执行命令，需要按下按键（）后，再输入命令。\
A. `#`；B. `'`；C. `:`；D. `$`

答案：C

#### 单选 38

目前光标在第 5 行，在命令模式下能够使光标回到第 2 行的命令是（）。\
A. `gg`；B. `3k`；C. `3j`；D. `4k`

答案：B

#### 单选 39

光标目前在第 2 行，删除第 2 行到第 4 行的命令是（）。\
A. `2-4d`；B. `3dd`；C. `dd3`；D. `D3`

答案：B

#### 单选 40

命令模式下，左移 2 个字的命令是（）。\
A. `2w`；B. `2b`；C. `2word`；D. `2e`

答案：B

#### 单选 41

在命令模式下，光标所在行只有一个单词 word，光标在字符 w 处。按下按键 R 后，输入 world，则此行的文本为（）。\
A. `wRworldord`；B. `world`；C. `wworldord`；D. `Rworldword`

答案：B

#### 单选 42

使用 `/` 或 `?` 进行搜索后，将光标移动到下一个匹配字符串，并且与搜索命令方向相同的按键是（）。\
A. `n`；B. `N`；C. `p`；D. `P`

答案：A

#### 单选 43

在 vi 中进行字符串替换的命令是（）。\
A. `a`；B. `i`；C. `o`；D. `s`

答案：D

#### 单选 44

vi 中进行字符串替换操作时，进行全行替换时的选项是（）。\
A. `c`；B. `g`；C. `l`；D. `a`

答案：B

#### 单选 58

直接使用命令 vi（不带任何参数）启动 vi 编辑器，想把编辑的内容写入到文件 filea 中，需要使用命令（）。\
A. `:e filea`；B. `:w filea`；C. `:r filea`；D. `:x filea`

答案：B（整理补充）。`:w 文件名` 把缓冲区内容写入指定文件。

#### 单选 59

使用 vi 编辑文件的过程中，想把缓冲区的内容写入到文件 otherfile 中去，使用的命令是（）。\
A. `:e otherfile`；B. `:r otherfile`；C. `:w otherfile`；D. `:x otherfile`

答案：C（整理补充）。理由同上题。

#### 单选 60

要将文件 fileb 的内容插入到当前编辑文件光标的下一行，可以使用命令（）。\
A. `:o fileb`；B. `:e fileb`；C. `:w fileb`；D. `:r fileb`

答案：D（整理补充）。`:r` 读入文件，插在光标所在行的下面。

#### 单选 61

vi 中想把文件中的第 3 到第 55 行写入文件 fileb，可以使用命令（）。\
A. `:3-55 w fileb`；B. `:3,55 w fileb`；C. `:3,55 cut fileb`；D. `:3-55 cut fileb`

答案：B（整理补充）。行范围用逗号隔开，写入用 `w`。

#### 单选 62

vi 中将光标移动到文件尾，需要使用命令（）。\
A. `$`；B. `G`；C. `T`；D. `gg`

答案：B（整理补充）。`gg` 是文件头，`$` 是行尾。

#### 单选 63

假设 X 为 vi 的一个选项，要查看该选项的当前值，可以使用命令（）。\
A. `:set X`；B. `:set X?`；C. `:set $X`；D. `:set &X`

答案：B（整理补充）。选项名后面加问号表示查询。

#### 判断 138（原稿注：bing 错了）

vi 打开文件后，以波浪号为开头的行表示该行尚未被编辑。

答案：对

#### 判断 139

命令模式下，按回车键或按键 j，都能使光标下移 1 行。

答案：对（整理补充）。

#### 判断 140

命令模式下，按键 Backspace 和按键 x 功能一致，都能删除字符。

答案：错（整理补充）。命令模式下 Backspace 只把光标左移，不删除字符。

#### 判断 141

命令模式下，按下按键 r 能够替换光标所在的字符并且切换到文本输入模式。

答案：错（整理补充）。`r` 替换一个字符后仍停在命令模式。

#### 判断 142

命令模式下，当光标位于单词第一个字母时，dw 命令可以用于删除光标所在的单词。

答案：对（整理补充）。

#### 判断 143

命令模式下 `/` 和 `?` 后接字符串都可以对字符串进行搜索，只是搜索方向不同。

答案：对（整理补充）。`/` 向下搜，`?` 向上搜。

#### 判断 144（原稿注：bing 错了）

vi 打开一个已存在的文件，文件会被复制到一个临时工作区，接着所进行的修改只作用于临时工作区中的副本，而不是原文件。

答案：对

#### 判断 156

vi 编辑器中，使用命令对选项的设置可以一直保存。

答案：错（整理补充）。在 vi 里用 `:set` 设置的选项只对本次编辑有效，想长期生效要写进配置文件（vi 用 `~/.exrc`，vim 用 `~/.vimrc`）。

## 五、文件系统与目录

#### 单选 45

存放 Linux 内核的文件的目录为（）。\
A. `/kernel`；B. `/boot`；C. `/root`；D. `/`

答案：B

#### 单选 46

已知文件 sda 为设备文件，它应该保存在目录（）中。\
A. `/device`；B. `/dev`；C. `/disk`；D. `/media`

答案：B

#### 单选 47

如果想了解内核的运行状态信息，可以通过查看目录（）中的相关文件。\
A. `/kernel`；B. `/boot`；C. `/proc`；D. `/root`

答案：C

#### 单选 48

root 用户登录后将进入（）目录。\
A. `/home/root`；B. `/root`；C. `/boot`；D. `/desktop`

答案：B

#### 单选 49

用户 tom 登录成功后，进入了他的主目录，此时他的工作目录为（）。\
A. `/usr/tom`；B. `/work/tom`；C. `/current/tom`；D. `/home/tom`

答案：D

#### 单选 50

用于存放配置文件的目录是（）。\
A. `/config`；B. `/register`；C. `/etc`；D. `/usr/local`

答案：C（整理补充）。

#### 单选 51

用户 root 登录成功后，发现在自己的主目录中，则 root 用户当前的工作目录是（）。\
A. `/work`；B. `/root/work`；C. `/root`；D. `/home/root`

答案：C（整理补充）。root 的主目录是 `/root`，不在 `/home` 下。

#### 单选 52

`/sys` 目录的父目录是（）。\
A. 没有父目录；B. sys；C. `/root`；D. `/`

答案：D（整理补充）。

#### 单选 53

`/usr/local` 是 `/usr` 的（）。\
A. 父目录；B. 工作目录；C. 子目录；D. 主目录

答案：C（整理补充）。

#### 单选 54

已知当前目录为 `/home/david/bin`，用户 david 执行 `cd ..` 命令后，再执行 pwd 命令，所显示的结果为（）。\
A. `/home/david/bin`；B. `/home/david`；C. `/home`；D. `/bin`

答案：B（整理补充）。`..` 是父目录。

#### 单选 55

`ls -l` 命令显示文件详细信息，第一个字段的第 2 到 10 个字符表示对文件不同用户的权限设置，其中表示对文件有执行权限的符号为（）。\
A. `e`；B. `r`；C. `x`；D. `g`

答案：C（整理补充）。

#### 单选 56

`ls -l` 命令显示文件详细信息，第一个字段的第 2 到 10 个字符表示对文件不同用户的权限设置，如果某个位置的符号为（），表示用户没有该项权限。\
A. `r`；B. `w`；C. `x`；D. `-`

答案：D（整理补充）。

![权限位按所有者、组用户、其他人分成三组，每组三个位置依次是读、写、执行，r、w、x 各占一个位置，横线表示该位置对应的权限没有](图/5-4_ls长格式权限位分组.png)

单选 55、56 都能从这张图读出来：`x` 落在每组第 3 位，表示执行；任何位置出现 `-` 都表示缺这一项权限。图下方表格的表头"代表文件类型"是课件照搬上一页留下的，应读作"权限含义"。

#### 单选 57

使用 rm 命令删除目录时，必须使用的选项为（）。\
A. `i`；B. `f`；C. `r`；D. `a`

答案：C（整理补充）。

#### 判断 145

UNIX 文件系统组织为一个以根目录（/）为根节点的树形结构。

答案：对（整理补充）。

#### 判断 146

系统日志等经常变动的文件放在 `/var` 目录中。

答案：对（整理补充）。

#### 判断 147

重要的、机密文件可以放置在 `/tmp` 目录中，阻止其他用户的访问。

答案：错（整理补充）。`/tmp` 是所有用户都能写入的临时目录。

#### 判断 148（原稿注：bing 错了）

绝对路径 `/home/david/bin` 中的第一个 `/` 表示路径开始，后面的 `/` 是父目录与子目录的分隔符。

答案：错。第一个 `/` 表示根目录。

#### 判断 149（原稿注：bing 错了）

使用绝对路径可以表示出文件系统中任意文件的位置，而使用相对路径只能表示出当前目录内部文件的位置。

答案：错。相对路径可以用 `..` 往上走，同样能表示任意位置。

#### 判断 150

在 UNIX 系统中，文件名中字母大小写不敏感。

答案：错（整理补充）。UNIX 文件名区分大小写。

#### 判断 151

cd 命令后面的参数，可以使用绝对路径的形式表示，也可以使用相对路径的形式表示。

答案：对（整理补充）。

#### 判断 152

mkdir 命令创建目录后，目录中有 `.` 文件和 `..` 文件。

答案：对（整理补充）。

#### 判断 153

rmdir 命令能够删除目录中只有 `.` 文件和 `..` 文件的目录。

答案：对（整理补充）。只有这两项的目录就是空目录。

#### 判断 154

`ls -l` 命令显示文件详细信息，第 5 个字段表示这个文件的大小，单位是 kb。

答案：错（整理补充）。单位是字节。

## 六、文件操作、重定向与通配符

#### 单选 64

输出重定向允许命令（进程）的输出保存到（）中。\
A. 标准输入设备；B. 标准输出设备；C. 文件；D. 标准出错设备

答案：C（整理补充）。

#### 单选 65

输入重定向允许命令（进程）从（）中获得输入。\
A. 标准输入设备；B. 标准输出设备；C. 文件；D. 标准出错设备

答案：C（整理补充）。

#### 单选 66

命令行中只输入 cat 命令，cat 命令从（）获得输入。\
A. 文件；B. 标准输入设备；C. 标准输出设备；D. 标准出错输出设备

答案：B（整理补充）。不给文件名时 cat 读键盘，按 Ctrl+D 结束。

#### 单选 67

cat 命令的选项（）能够显示文件行号。\
A. `-r`；B. `-i`；C. `-n`；D. `-f`

答案：C（整理补充）。

#### 单选 68

用于创建文件副本的命令是（）。\
A. copy；B. cp；C. mv；D. ln

答案：B（整理补充）。

#### 单选 69

如果使用 cp 命令复制目录，需要使用选项（）。\
A. `-i`；B. `-f`；C. `-r`；D. `-b`

答案：C（整理补充）。

#### 单选 70

用于将文件从一个地方移动到另外一个地方的命令是（）。\
A. mv；B. move；C. cp；D. ln

答案：A（整理补充）。

#### 单选 71

将文件 filea 改名为 fileb 的命令是（）。\
A. `cn filea fileb`；B. `mv filea fileb`；C. `cp filea fileb`；D. `ln filea fileb`

答案：B（整理补充）。

#### 单选 72

mv 命令的选项（）能够对命令的执行进行解释。\
A. `-b`；B. `-r`；C. `-v`；D. `-i`

答案：C（整理补充）。`-v`（verbose）显示执行过程。

#### 单选 73

命令（）能够在已存在文件和新文件之间建立链接。\
A. cp；B. mv；C. ln；D. lk

答案：C（整理补充）。

#### 单选 74

wc 命令统计行数的选项是（）。\
A. `lines`；B. `w`；C. `l`；D. `c`

答案：C（整理补充）。

#### 单选 75

wc 命令统计字数的选项是（）。\
A. `c`；B. `count`；C. `w`；D. `word`

答案：C（整理补充）。

#### 单选 76（原稿题号标红）

下面命令中能显示出当前目录中文件（不包括隐藏文件）个数的是（）。\
A. `ls -l | wc -l`；B. `ls -l | wc -w`；C. `ls | wc -w`；D. `ls | wc -l`

答案：D（整理补充）。ls 的输出送进管道时每行一个文件名，`wc -l` 数行数就是文件个数。A 会多数"总计"那一行；C 遇到含空格的文件名会多数。

#### 单选 77

使用 find 命令将在 cpp1 目录中找到以 .c 为结尾的文件并且将它们都拷贝到目录 cpp2 中的命令是（）。\
A. `find cpp1 -name "*.c" -exec cp cpp2 {} \;`\
B. `find cpp1 -name "*.c" -exec cp {} cpp2 \;`\
C. `find cpp1 -name *.c -exec cp cpp2 {} \;`\
D. `find cpp1 -name *.c -exec cp {} cpp2 \;`

答案：B（整理补充）。cp 要源在前、目标在后，`{}` 代表找到的文件；`*.c` 要加引号，防止 shell 先把它展开。

#### 单选 78（原稿题号标红）

显示文件 filea 第 21 到 30 行的命令是（）。\
A. `head -n 21-30 filea`；B. `tail -n 30-21 filea`；C. `head -30 filea | tail -10`；D. `head -30 filea | tail -21`

答案：C（整理补充）。先取前 30 行，再取这 30 行里的最后 10 行，正好是第 21 到 30 行。

#### 单选 79

使用 cut 命令从文件中取出指定域的选项是（）。\
A. `f`；B. `d`；C. `c`；D. `t`

答案：A（整理补充）。

#### 单选 80

使用 cut 命令取出指定字符的选项是（）。\
A. `f`；B. `d`；C. `c`；D. `t`

答案：C（整理补充）。

#### 判断 155

cat 命令的参数可以为一个或多个文本文件名，用来显示这个/些文件的内容。

答案：对（整理补充）。

#### 判断 157

文件 filea 已存在，执行命令 `ls >>filea` 后，filea 原有的数据不会被覆盖。

答案：对（整理补充）。`>>` 是追加。

#### 判断 158

执行命令 `cal > this_year` 后，屏幕上不会显示 cal 命令的输出内容。

答案：对（整理补充）。输出都写进了文件。

#### 判断 159

使用 cat 命令，结合输出重定向可以创建新文件。

答案：对（整理补充）。`cat > 文件` 后输入内容，按 Ctrl+D 结束。

#### 判断 160

dir 是当前目录中一个已存在的目录，`cp filea dir` 命令成功执行后，会生成文件 filea 的副本，名为 dir。

答案：错（整理补充）。副本放进目录里，是 `dir/filea`。

#### 判断 161

作为 cp、mv 和 ln 命令参数的文件名，既可以用绝对路径表示，也可以用相对路径表示。

答案：对（整理补充）。

#### 判断 162

dir 是当前目录中的一个目录，当前目录中不存在目录 dir1，`mv dir dir1` 命令执行的结果是目录 dir 被移动到目录 dir1 中。

答案：错（整理补充）。dir1 不存在时，结果是 dir 改名为 dir1。

#### 判断 163

使用命令 `mv filea dir`，将文件 filea 移动到 dir 目录中后，文件 filea 的名字不变。

答案：对（整理补充）。

#### 判断 166

对于 wc 命令来说，字之间的分隔符是空格或 tab（制表符）。

原卷无答案。提示：wc 统计字数时，空格、制表符和换行符都起分隔作用。

#### 判断 167

wc 命令能够从标准输入设备读取数据并进行统计。

答案：对（整理补充）。

#### 判断 168

用作通配符时，`?` 可以匹配文件名中的 0 个或者 1 个字符。

答案：错（整理补充）。`?` 恰好匹配 1 个字符。

#### 判断 169

可以使用 `ls *.*` 显示当前目录下的所有文件。

答案：错（整理补充）。`*.*` 只匹配名字里带点的文件。

#### 判断 170

`rm *` 表示要删除当前目录中的所有文件。

原卷无答案。提示：`*` 不匹配以 `.` 开头的隐藏文件，子目录不加 `-r` 也删不掉。

#### 判断 171

文件名中的通配符由命令自己识别，shell 无需处理。

答案：错（整理补充）。通配符由 shell 展开后再交给命令。

#### 判断 172

通配符 `[]` 出现的位置可以没有字符。

答案：错

#### 判断 173

find 命令的 `-name` 选项后面出现带通配符的文件名时，需要将整个文件名用双引号引上。

原卷无答案。提示：带通配符的文件名要加引号，防止 shell 在 find 运行前就把它展开，单引号、双引号都起作用。

#### 判断 174

动作选项 `-exec` 和 `-ok` 的区别在于，`-exec` 用户执行命令前需要确认，而 `-ok` 不需要。

答案：错（整理补充）。说反了，`-ok` 执行前要确认，`-exec` 不需要。

#### 判断 175

对于 cut 命令来说，文本中域默认分隔符是空格。

答案：错（整理补充）。默认分隔符是制表符。

#### 判断 176

more 命令可以分页查看文本文件，但是不能向前翻页。

原卷无答案。提示：util-linux 的 more 查看文件时按 `b` 可以往回翻一屏，数据来自管道时不行；less 前后都能翻。

#### 判断 177

命令 `ls | more`，可以将当前目录中的文件分页显示出来。

答案：对（整理补充）。

## 七、索引节点与链接

#### 判断 164

已知 fileb 是 filea 的链接文件，删除文件 fileb 后，filea 的链接数属性值减一。

答案：对（整理补充）。按 ln 默认建立的硬链接理解：两个名字共用一个索引节点，删掉一个名字，链接数减一。如果 fileb 是 `ln -s` 建立的符号链接，删除它不会改变 filea 的链接数。

#### 判断 165

修改链接文件的内容，不影响被链接文件的内容。

答案：错（整理补充）。链接文件和被链接文件访问的是同一份数据。

#### 判断 178

UNIX 系统将每一个文件与一个整数（i 节点号）相关联，并用文件的 i 节点号来标识每个文件。

答案：对（整理补充）。

#### 判断 179

所有的索引节点号和对应的文件基础信息保存在索引节点表中，存储在磁盘中。

答案：对（整理补充）。

#### 判断 180

复制命令所产生的新文件和原文件索引节点号相同。

答案：错（整理补充）。复制出来的是新文件，有新的索引节点号。

#### 判断 181

使用 mv 命令将文件从一个目录移动到另一个目录，文件索引节点号也随之变化。

答案：错（整理补充）。在同一个文件系统内移动只改目录项，索引节点号不变。

#### 判断 182

使用 mv 命令将文件改名，文件的索引节点号会随着文件名的改变而改变。

答案：错（整理补充）。

#### 判断 183

使用 ln 命令为文件 filea 创建链接文件，filea 的索引节点号不变，但文件的数据会被复制一次。

答案：错（整理补充）。ln 不复制数据，链接和原文件共用同一个索引节点。
